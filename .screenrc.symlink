vbell off
autodetach on
startup_message off
bell_msg ""

altscreen off
defscrollback 10000

bind - resize -3
bind = resize +3
bind + resize =

# keep calm, if it doesn’t exist, nothing will happen!
source ~/.screenrc.local
