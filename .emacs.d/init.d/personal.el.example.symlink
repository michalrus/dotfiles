;; -*- mode: emacs-lisp -*-

;; whoami
(setq user-mail-address "m@michalrus.com")
(setq user-full-name "Michal Rus")

;; irc
(defun weechat ()
  (interactive)
  (weechat-connect "michalrus.com" 9001 nil 'ssl))
