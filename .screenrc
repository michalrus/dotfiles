vbell off
autodetach on
startup_message off
bell_msg ""

altscreen on
defscrollback 10000

# escape ^Ee

bind - resize -3
bind = resize +3
bind + resize =

shell "/bin/bash"

screen -t 'ssh'       0 bash
screen -t '~'         1 bash -c 'cd ~ ; exec bash'
