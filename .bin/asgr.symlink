#!/bin/sh

if [ $# -lt 2 ] ; then
  echo "Usage: $0 <group> <executable> [<arg1>...]"
  exit 1
fi

_group="$1"
shift
_args="$(getopt --options "" --shell sh -- -- "$@" | dd ibs=1 skip=3 2>/dev/null)"

exec sg "$_group" -c "$_args"
