#!/bin/sh

command -p git --version >/dev/null || exit $?

if [ "$1" = "pull" ] ; then
  echo 'git: '\''pull'\'' is deliberately disabled.'
  echo
  echo 'Merge/rebase manually after issuing:'
  echo '	git fetch'
  exit 1
fi

command -p git "$@"
