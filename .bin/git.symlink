#!/bin/sh

git="$(which -a git | grep -v '/.bin/git$' | head -n 1)"
[ -z "$git" ] && git=/usr/bin/git

if [ "$1" = "pull" ] ; then
  echo 'git: '\''pull'\'' is deliberately disabled.'
  echo
  echo 'Merge/rebase manually after issuing:'
  echo '	git fetch'
  exit 1
else
  exec "$git" "$@"
fi
