#!/bin/sh

daemonize=daemonize
type $daemonize 1>/dev/null 2>&1 || daemonize=exec

# NixOS doesn’t respect these…
$daemonize $HOME/.bin/command-p chromium --no-default-browser-check $CHROMIUM_USER_FLAGS "$@"
